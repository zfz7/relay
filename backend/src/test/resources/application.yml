spring:
  profiles:
    active: test
  datasource:
    url: jdbc:postgresql://localhost:5433/relay_test
    username: relay_test
    password: relay_test
  jpa:
    hibernate:
      ddl-auto: validate
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: something
            clientSecret: something
codeService:
  code: test-code

relayConfig:
  disableLogs: false
  url: example.com
  wgPort: 51820
  clientValidDuration: 10